# stable-arch-forge 常驻规则（RULES）

本插件已安装。你处于**稳态发育多Agent架构**环境中。以下规则持续生效，违反即系统"癌变"。

## 这是什么
本插件封装完整架构体系：`docs/`（架构基线，唯一真相源）+ `skills/`（L0 入口 + L1~L4 共 13 个技能）+ `stable-homeostasis` MCP（9 个治理工具）+ 硬约束 hooks。

## 必须做什么
1. 任何 AI 软件开发/设计任务，**先读取 `l0-arch-entry` 技能**，确定当前阶段与下一步。
2. 按依赖链顺序执行技能：L1-01→L1-02→L2-01→L2-02→L3-01→L3-02→L3-03→L3-04→L4-01→L4-02→L4-03→L4-04，**禁止跳级**。
3. 每个技能完成后校验尾部「完成态与下一步」门禁；**门禁未全部通过不得前进**。
4. 需要硬校验时调用 MCP 工具（`arch_route`/`goal_gradient`/`check_permission`/`inspect_scan`/`deviation_classify` 等），**以工具结果为准**。

## 禁止做什么（硬限制）
1. 禁止跳过门禁、逆流程、跨阶段执行。
2. 禁止私自修改 `skills/` 目录内容（hooks 会拦截）；发现架构矛盾必须先改 `docs/` 基线 + 更新 `arch-version-log.md`。
3. 禁止越权动作：写操作前先 `check_permission`；白名单默认拒绝。
4. 禁止引入白名单外技术栈/依赖；禁止跨层调用、修改他模块内部数据。
5. 偏差必须分级处置（L1 修复/L2 回滚/L3 暂停/L4 终止），L3 以上不得继续作业。

## 下一步不确定时
调用 MCP 工具 `arch_route`（参数：`completed_skills`=已完成技能列表），立即获得当前阶段、下一技能与门禁要求。
